//
// IsOwnerForProjectBySlug.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct IsOwnerForProjectBySlug: Codable, JSONEncodable, Hashable {

    /** ProjectSlug is the project's slug. */
    public var projectSlug: String
    /** Subject is the subject from the API Token. */
    public var subject: String

    public init(projectSlug: String, subject: String) {
        self.projectSlug = projectSlug
        self.subject = subject
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case projectSlug = "ProjectSlug"
        case subject = "Subject"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(projectSlug, forKey: .projectSlug)
        try container.encode(subject, forKey: .subject)
    }
}

